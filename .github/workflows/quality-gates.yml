name: Quality Gates

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  quality-gates:
    uses: IggyIkenna/unified-trading-pm/.github/workflows/python-quality-gates.yml@live-defi-rollout
    with:
      dep_repos: "unified-trading-library unified-config-interface unified-events-interface unified-internal-contracts unified-cloud-interface"
    secrets:
      GH_PAT: ${{ secrets.GH_PAT }}
